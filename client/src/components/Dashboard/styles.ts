import { makeStyles } from '@material-ui/core/styles';

export const ListGridStyle = makeStyles({
  root: {
    maxHeight: '50vh',
    overflow: 'auto',
  },
});

export const LoadingContainerStyle = makeStyles({
  root: {
    height: '10px',
    marginBottom: '10px',
  },
});
